apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  labels:
    app: catalog-service
spec:
  type: LoadBalancer
  selector:
    app: catalog-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
